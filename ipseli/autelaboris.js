// Expanded function for clarity
function displayMessage() {
  console.log('Hello, world!');
}

// Optimized with clear function naming and using const for immutability
const delayMilliseconds = 1000;

setTimeout(displayMessage, delayMilliseconds);
